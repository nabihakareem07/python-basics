count = 0
sum = 0

while True:
    num = float(input("Enter a number: "))

    if num == -1:
        break

    sum = sum + num
    count = count + 1

if count > 0:
    average = sum / count
    print("Count =", count)
    print("Average =", average)
else:
    print("No numbers entered")