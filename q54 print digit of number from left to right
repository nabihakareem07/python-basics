num = int(input("Enter a number: "))

divisor = 1
temp = num

while temp >= 10:
    temp = temp // 10
    divisor = divisor * 10

while divisor > 0:
    digit = num // divisor
    print(digit)
    num = num % divisor
    divisor = divisor // 10